<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollegeCanteen"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.MenuActivity" />
        <activity android:name=".ui.CartActivity" />
        <activity android:name=".ui.OtpVerificationActivity" />
        <activity android:name=".ui.StaffDashboardActivity" />
        <activity android:name=".ui.OrderConfirmationActivity" />
        <activity android:name=".ui.OrderHistoryActivity" />
        <activity android:name=".ui.ProfileActivity" />

    </application>

</manifest>
